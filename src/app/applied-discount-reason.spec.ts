import { AppliedDiscountReason } from './models/applied-discount-reason';

describe('AppliedDiscountReason', () => {
  it('should create an instance', () => {
    expect(new AppliedDiscountReason()).toBeTruthy();
  });
});
